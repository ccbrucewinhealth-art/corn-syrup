# 需求文件：通知與即時互動

產出時間：2026-05-02 09:18:40 +0800

## 需求編號

REQ-FE-04

## 需求來源

- 參考系統：`/home/ccbruce/public/github/uptime-kuma/src`
- 目標系統：`src/frontend`
- 階段範圍：通知 provider 表單、NotificationDialog、Socket 即時事件與通知設定。

## 功能需求

1. 需依參考系統 Vue 原始碼意圖，轉換為 React/TypeScript 實作。
2. 需維持使用者可觀察的路由、文案、互動流程與資料欄位語意。
3. 需納入 i18n，至少支援 `zh-TW`、`en`、`ja`，預設 `zh-TW`。
4. 對來源為純資產或資料檔者，需在轉換檢查表標示不轉換並保留對應。
5. 單檔接近 500 行時，需依功能拆分至具語意名稱的檔案。

## 驗收條件

- 相關來源檔案均列入 `80.checklist/80.translate_check-list.md`。
- 目標 React 檔案可被 TypeScript 與 Vite 專案引用。
- 對應 shell 與 Makefile 可支援安裝、建置、開發與離線套件準備。
